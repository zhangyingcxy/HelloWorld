<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
package="com.swufe.helloworld">

<uses-permission android:name="android.permission.INTERNET" />

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.HelloWorld"
    android:usesCleartextTraffic="true">
    <activity android:name=".SettingsActivity"></activity>
    <activity android:name=".FragmentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
    </activity>
    <activity android:name=".GridViewActivity" />
    <activity android:name=".MyListActivity2"></activity>
    <activity
        android:name=".SettingsActivity"
        android:label="@string/settings_title"
        tools:ignore="DuplicateActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.swufe.helloworld.FragmentActivity" />
    </activity>
    <activity android:name=".MainActivity" />
    <activity android:name=".RateCalculate" />
    <activity android:name=".MyListActivity" />
    <activity android:name=".RateListActivity"></activity>
    <activity android:name=".AutoRateActivity" />
    <activity android:name=".VoteActivity"></activity>
</application>
</manifest>
